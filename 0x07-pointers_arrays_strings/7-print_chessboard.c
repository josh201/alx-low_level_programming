#include "main.h"

/**
  * print_chessboard - a function that 
  * @a: an array input to print
  * Return: Nothing
  */

void print_chessboard (*a)[8])
{
	int i, j;

	for (i = 0; i < 8; i++)
	{
		for (j = 0; j < 8; i++)
			_putchar(a[i][j]
					_putchar('\n';
						}
						]
